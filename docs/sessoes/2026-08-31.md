# Sessão 2026-08-31

## Manutenção — navbar lateral esquerda (sidebar)

**User pediu:** As abas de "Visão", "Manutenção", Insumos, "Ordens de Serviço", Preventiva, Financeiro e Config devem ficar escondidas em uma aba lateral esquerda. Ao clicar em um item, deve puxar as informações daquela seção.

**Feito (Manutencao.jsx):**
- Convertida a navbar horizontal agrupada em **sidebar lateral esquerda fixa** (`aside`, classe `.manut-sidebar`, style `s.navGroups`): `position:fixed; left:0; top:64; bottom:0; width:250px; overflowY:auto`, grupos empilhados em coluna com separador `border-bottom`.
- Conteúdo (KPIs `s.kpisRow` e `s.main`) deslocado p/ a direita: `marginLeft:262` (e override `veiculo` `marginLeft:262`).
- Header ganhou botão **"Menu"** (`s.sidebarToggle`, `<Ico.Menu>` novo) que abre a sidebar; cabeçalho da sidebar com título "Seções" + botão fechar (`.manut-sidebar-close`).
- **Mobile (≤960px):** sidebar vira **drawer overlay** deslizante (`transform: translateX(-100%)` → `.is-open` `translateX(0)`) + fundo `.manut-sidebar-backdrop`; content `marginLeft` zera.
- `onClickCapture` no aside fecha a sidebar ao selecionar item (essencial no mobile).
- Ícone `Menu` adicionado ao objeto `Ico` (lucide-style).

**Arquivos:** frontend/src/pages/Manutencao.jsx
**Resultado:** OK — compilado limpo via Vite (HTTP 200 / módulo transformado sem erro).

## Retrabalho — sidebar e KPIs (organização visual)

**User pediu:** "Mal estruturado. Os botões não ficaram organizados. Algumas informações ficaram amadoras."

**Problema:** NavTab era botão-pílula horizontal (border-radius 10, `accent` aleatório por item: #0891b2, #2563eb, #dc2626, #7c3aed; hover via JS inline) — ficou desorganizado na sidebar.

**Feito (Manutencao.jsx):**
- `NavTab` redesenhado: botões **full-width** (`width:100%`) como linhas da sidebar, com barra esquerda de acento (`borderLeft:3px`) no item ativo, fundo `var(--accent-soft)` + texto `var(--accent)`, peso 700 ativo / 600 normal. Hover agora via CSS `.manut-snav-item:hover` (em vez de JS).
- Ícones: item inativo usa cor semântica (accent do grupo) e ativo usa `var(--accent)` uniforme.
- Labels de seção `.navGroup`/`.navGroupLabel` polidos (uppercase, peso 800, letter-spacing .09em, padding alinhado 12px).
- KPIs (`s.kpi`) repaginados: card moderno (round 14, padding 16/18, sombra), ícone 34px, valor grande 1.6rem; adicionada **faixa superior colorida** por card + hover lift via CSS `.manut-kpis > button`.
**Arquivos:** frontend/src/pages/Manutencao.jsx
**Resultado:** OK — compilado limpo (HTTP 200).

## Remoção do botão "Menu" + sidebar persistente organizada

**User pediu:** "Remover botão com termo 'Menu' e navbar vertical esquerda deve ser organizada melhor."

**Feito (Manutencao.jsx):**
- **Removido** o botão "Menu" do header (e toda a maquinaria de drawer: estado `sidebarAberto`, backdrop, botão fechar ×, toggle CSS).
- Sidebar agora é **sempre visível** (persistente) em todas as telas — não precisa de botão para abrir. No desktop 250px; em ≤1200px afina para 218px (conteúdo offset ajustado p/ 230px).
- **Sidebar reorganizada/mais limpa:**
  - Título do módulo no topo: "Manutenção" com sublinhado de acento.
  - Grupos (Visão, Manutenção, Insumos, OS, Preventiva, Financeiro, Config) com rótulos uppercase peso 800 e divisórias suaves.
  - Itens `NavTab` como linhas full-width (sem borda-pílula), hover via CSS, ativo com fundo `--accent-soft` + texto accent.
  - Badges alinhados à direita (`marginLeft:auto`).
**Arquivos:** frontend/src/pages/Manutencao.jsx
**Resultado:** OK — compilado limpo (HTTP 200).

## Limpeza total + design uniforme (módulo Manutenção)

**User pediu:** "Código total deve ser limpo e design deve ser todo uniforme." Escolheu escopo: Manutenção (shell + conteúdo das abas).

**Feito:**
- **Migração de cores hex → tokens** (design claro/escuro) em TODO o módulo:
  - 9 Abas (`/manutencao/Aba*.jsx`): ~380 substituições (AbaEstoque 72, AbaMultas 59, AbaIndicadores 44, AbaControleRotina 38, AbaTimeline 35, AbaVistoria 29, AbaRequisicoes 55, AbaConjuntoVencimentos ~20, AbaPreditiva 14).
  - `Manutencao.jsx`: corpo de render (~2801–4651) + componentes (~100–1050) + modais (~4652–5651) ≈ +400 substituições. Hex total caiu de **433 → 114** (restantes intencionais: paletas `PALETA_CAT`/`SUB_COR`, gradiente de marca `#1a3a5c→#234775`, white-on-brand, tons de status fora do dicionário).
  - Sufixos alpha concatenados (ex.: `${cor}22`) → `var(--border)`/tokens `-bg`.
- **Código morto removido** do `const s` do Manutencao.jsx: `tabBar`, `tab`, `tabAtivo`, `tabBadge`, `navTab`, `navTabActive`, `navTabBadge`, `grupoSection`, `tipoGrid`, `tipoCard`, `tipoCardTop`, `tipoNome`, `tipoDesc`, `tipoMeta`, `tipoVazio`, `acoes` (0 usos).
- KPIs/icons/`alertaBadge`/`erroMsg`/botões → tokens semânticos (`--danger`, `--success`, `--info`, `--tech`, `--chart-6`).

**Arquivos:** frontend/src/pages/Manutencao.jsx, frontend/src/manutencao/*.jsx (9 abas)
**Resultado:** OK — todos compilam HTTP 200; design uniforme (superfícies, bordas, texto, tabelas, inputs, botões, cards, modais via tokens).

## Limpeza de arquivos mortos na VPS

**User pediu:** "Acessar VPS e excluir arquivos mortos."

**Acesso SSH:** chave `~/.ssh/pontual_homolog` → `root@72.60.8.135` (host `pontualpetroleo`). VPS Hostinger KVM, `/var/pontual`.

**Escopo confirmado pelo user:** remover tarballs raiz antigos em `/var/pontual/` (backup-prod, deploy, dist, frontend-bak).

**Removido (41 arquivos, ~1,1 GB):** `backup-prod-*.tar.gz` (28×, 10–31/ago), `backend-deploy.tar.gz`, `dist.tar.gz`, `frontend-bak-20260730-150010.tar.gz`, `backup-lancamentos-os-*.js.bak`, `backup-prod-antes-remover-*.sql.gz`, `backup-prod-pre-pasta1-*.sql.gz`. `/var/pontual` caiu de **7,1 GB → 6,1 GB**.

**Mantido (fora do escopo):** `backups/` (dumps diários do cron `backup-nightly.sh`), `backend/`, `frontend/`, `logs/`, `uploads/`, `schema*.sql` e `schema.tar.gz`.

**Pós-check:** `pontual-backend` e `pontual-backend-homol` online (PM2), apache2 active. Nenhum serviço impactado.

**Arquivos:** VPS `/var/pontual/` (não versionado no git)
**Resultado:** OK — limpo, serviços saudáveis.

## Copia fiel localhost (dump VPS -> Postgres local)

**Objetivo:** montar copia fiel localhost: sincronizar backend/schema do repo local com a VPS e copiar os DADOS REAIS (dump do banco) da VPS para o Postgres localhost.

**Schemas/rotas/services baixados da VPS:** ackend-vps/schema/ (005-014 + 999-restore, 10 novos), src/routes/ (contratos, fornecedores, viagens), src/services/ (automacao-viagens, contrato-parser, pdf-autorizacao), package.json da VPS + 
pm install (22 pacotes).

**Dump do banco:** criado pontual-full-dump.sql na VPS (6.986 KB, 13 tabelas com COPY) e transferido via scp para temp local. Filtradas 2 linhas OWNER TO postgres (exigiam superuser). Drop do banco/public local e restauracao do dump no v17 local como pontual_app (exit 0; 41 indexes, view, 9 triggers).

**Validacao de paridade (contagem VPS == local):** veiculos 94==94, manutencoes 782==782, documents 2870==2870, ordens_servico 264==264, lancamentos_os 231==231, usuarios_auth 6==6, contratos_compra/fornecedores/viagens 0==0 (vazias na prod tambem).

**Backend local:** start 
ode src/index.js (porta 3000, AUTH_MODE=jwt, backend.log). Login JWT OK via usuario teste local; GET /api/veiculos retorna 94 veiculos reais (placas, chassi, renavam, bloqueios OS).

**Frontend local:** .env = VITE_DATA_BACKEND=vps + VITE_PONTUAL_API_URL=http://localhost:3000. Vite reiniciado em 5173, servindo app HTTP 200; app ja faz requests ao backend local (veiculos/manutencoes/ordens-servico 200/304; /api/auth/me 401 = logado nao).

**Nota:** criei usuario teste local (	este.local@pontual.local / TesteLocal123!, super admin) so para validar login JWT no .env local. Credenciais reais dos admins de producao desconhecidas (necessario login do user na UI local). Objeto de teste pode ser removido depois.

**Arquivos:** backend-vps/.env, backend-vps/package.json, backend-vps/src/routes/*, backend-vps/src/services/*, backend-vps/schema/*, frontend/.env, temp/pontual-full-dump.sql(/filtrado)
**Resultado:** OK - stack local completa servindo dados reais.

## Documentação técnica em Word (.docx)

**User pediu:** "documentação técnica deve ser elaborada em word".

**Escopo (confirmado pelo user):** todos os 6 tópicos — Visão geral/arquitetura, Instalação/configuração, Modelo de dados/banco, API/endpoints, Migração Firebase→VPS, Guia de operação/manutenção.

**Ferramenta:** python-docx (instalado via pip). Gerado por script de C:\Users\ADMINI~1\AppData\Local\Temp\opencode\gerar_docs.py.

**Documento gerado:** docs/Documentacao-Tecnica-Pontual.docx (46.6 KB, 314 parágrafos, 7 tabelas, capa + sumário auto-atualizável + 6 capítulos H1).

**Conteúdo** coletado diretamente do código/banco reais:
- Stack: React 19 + Vite 8 + Node/Express + PostgreSQL (16 VPS/17 local) + JWT + PM2 + Apache + SASCAR/CTA.
- 13 tabelas reais via information_schema (veiculos, manutencoes, ordens_servico, lancamentos_os, tipos_manutencao_custom, documents, usuarios_auth, fornecedores, contratos_compra, contratos_anexos, viagens, viagem_itens, viagens_anexos + view v_contratos_saldo).
- Todos os endpoints REST das rotas (auth, collections whitelist, veiculos, manutencoes, ordens-servico, lancamentos-os, tipos-manutencao, contratos, fornecedores, viagens, sascar, jornada, cta, uploads, intranet-gate).
- Migração Firebase→VPS (2026-07-23), camada pontualApi.js como ponto único, Supabase como backend alternativo via VITE_DATA_BACKEND.
- Guia: dump/restauração, cópia fiel VPS→localhost, PM2, cronjobs, limpeza, diagnóstico, segurança.

**Arquivos:** docs/Documentacao-Tecnica-Pontual.docx, temp/gerar_docs.py
**Resultado:** OK - documento Word completo gerado.
